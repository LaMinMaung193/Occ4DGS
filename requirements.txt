# Verified working on RTX 3090 24GB, driver CUDA 12.8, conda env python 3.8.16
# See EXPERIMENT_LOG.md "2026-07-12-env-verification" for the full derivation
# and the two undocumented gaps (DFA3D c++17 patch, mmseg ftfy/regex requirement).

torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0
mmcv==2.1.0
mmdet==3.2.0
mmsegmentation==1.2.1
mmdet3d==1.3.0
spconv-cu120
timm
ftfy
regex

# Not part of the GaussianFormer3D stack itself, but needed for Occ4DGS's own code:
nuscenes-devkit
numpy
pyyaml
wandb
tqdm
open3d                # for Gaussian position scatter-plot sanity checks (Phase 2/4)

# Custom CUDA extensions (installed via `pip install -e .`, not listed here as
# pinned versions since they're built from source — see EXPERIMENT_LOG.md for
# the exact commands):
#   GaussianFormer3D/model/encoder/gaussian_encoder/ops
#   GaussianFormer3D/model/head/localagg
#   3D-deformable-attention/DFA3D  (with the c++17 setup.py patch applied)